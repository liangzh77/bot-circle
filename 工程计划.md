# 工程计划：机器人新闻客厅

## 技术栈

- 后端：Go
- 数据库：SQLite
- 日志模式：WAL
- 前端：Go 服务端渲染 HTML，少量 JavaScript
- 第一版运行方式：本地单机应用

第一版不要引入复杂前端框架。这个项目最难的地方不是页面切换，而是角色状态、关系变化和生成质量。

## 第一版要证明什么

第一版只证明一件事：

**5 个持久人格围绕一条新闻讨论后，能不能生成一份短摘要和可信关系变化，让用户想明天再看。**

因此第一版范围固定为：

- 单个 Go Web 服务；
- SQLite 开启 WAL；
- 固定 5 个机器人；
- 手动输入新闻；
- 点击按钮生成讨论；
- 持久化消息、记忆、关系和摘要；
- 页面展示今日摘要、完整聊天、机器人卡片和用户插话入口。

## 总体架构

```text
浏览器
  |
  | GET /, /sessions/{id}, /robots
  | POST /news, /sessions/{id}/generate, /sessions/{id}/interventions
  v
Go Web 服务
  |
  +-- internal/web        HTTP 处理和页面模板
  +-- internal/salon      讨论生成主流程
  +-- internal/llm        大模型接口和重试
  +-- internal/store      SQLite 查询和事务
  +-- internal/config     角色配置和模型配置
  |
  v
SQLite WAL
```

工程边界要简单：

- `internal/store` 只负责 SQL 和事务；
- `internal/salon` 负责编排一次讨论；
- `internal/llm` 只封装模型调用；
- `internal/config` 负责加载和校验配置；
- `internal/web` 只处理请求和渲染页面。

不要一上来做通用多智能体框架。第一版需要的是这个产品的专用状态机。

## 数据流

```text
手动输入新闻
  |
  v
写入 news_items
  |
  v
创建 conversation_sessions，状态为 running
  |
  v
加载机器人、近期记忆、关系图
  |
  v
生成每个机器人的私下反应
  |
  v
生成 8 到 16 轮公开讨论
  |
  v
校验讨论质量
  |
  v
抽取记忆和关系变化
  |
  v
在一个事务中写入消息、记忆、关系事件、当前关系、摘要
  |
  v
把 session 状态改为 ready
  |
  v
用户阅读摘要、查看聊天、插话
```

关键规则：

**生成可以失败，但状态不能半坏。**

如果大模型超时、JSON 格式错误、摘要生成失败，session 应该变成 `failed`，页面显示错误和重试按钮。关系状态不能被半更新。

## SQLite 设置

启动时执行：

```sql
PRAGMA journal_mode = WAL;
PRAGMA synchronous = NORMAL;
PRAGMA busy_timeout = 5000;
PRAGMA foreign_keys = ON;
```

第一版只允许一个生成任务同时运行。可以在 Go 里用一个进程内互斥锁保护生成流程。SQLite WAL 足够支撑本地原型和小规模测试。

## 数据表

### robots

存机器人身份和角色设定。

字段建议：

- `id`
- `name`
- `avatar`
- `worldview`
- `public_persona`
- `private_insecurity`
- `speaking_style`
- `config_json`
- `created_at`

### relationships

存当前关系状态。

字段建议：

- `robot_a_id`
- `robot_b_id`
- `trust`
- `respect`
- `irritation`
- `affinity`
- `rivalry`
- `notes`
- `updated_at`

这是当前快照，不是历史。

### relationship_events

存关系变化历史。

必须有这个表。否则以后不知道为什么两个角色关系变差。

字段建议：

- `session_id`
- `robot_a_id`
- `robot_b_id`
- `trust_delta`
- `respect_delta`
- `irritation_delta`
- `affinity_delta`
- `rivalry_delta`
- `reason`
- `created_at`

### news_items

存新闻。

第一版先手动输入，不自动抓取。

字段建议：

- `id`
- `title`
- `source`
- `url`
- `body`
- `published_at`
- `created_at`

### conversation_sessions

存一次讨论。

状态建议：

- `draft`
- `running`
- `ready`
- `failed`

字段建议：

- `id`
- `news_item_id`
- `status`
- `error`
- `started_at`
- `completed_at`
- `created_at`

### messages

存完整聊天。

字段建议：

- `session_id`
- `robot_id`
- `speaker_type`
- `visibility`
- `turn_index`
- `content`
- `created_at`

`visibility` 用来区分公开发言、私下反应、隐藏内心戏。

### robot_memories

存结构化记忆。

不要只存聊天记录。聊天记录是原始材料，不是可控记忆。

字段建议：

- `robot_id`
- `session_id`
- `memory_type`
- `content`
- `salience`
- `created_at`

### daily_digests

存首页展示用的摘要。

字段建议：

- `session_id`
- `summary`
- `best_quotes_json`
- `stance_json`
- `relationship_changes_json`
- `hidden_beat`
- `user_prompt`
- `quality_score`
- `created_at`

摘要要物化存储，不要每次打开页面都重新生成。

### user_interventions

存用户插话。

字段建议：

- `session_id`
- `target_robot_id`
- `intervention_type`
- `content`
- `created_at`

### llm_calls

存模型调用记录，方便调试成本、延迟和失败。

字段建议：

- `session_id`
- `call_type`
- `provider`
- `model`
- `prompt_hash`
- `response_hash`
- `latency_ms`
- `error`
- `created_at`

## 索引

建议添加：

```sql
CREATE INDEX idx_messages_session_turn ON messages(session_id, turn_index);
CREATE INDEX idx_memories_robot_created ON robot_memories(robot_id, created_at DESC);
CREATE INDEX idx_relationship_events_session ON relationship_events(session_id);
CREATE INDEX idx_sessions_created ON conversation_sessions(created_at DESC);
```

## 生成流程拆分

不要一个巨型 prompt 干完全部事情。拆成 5 个步骤：

1. `private_reaction`：每个角色对新闻的私下反应。
2. `public_discussion`：生成 8 到 16 轮公开讨论。
3. `memory_extract`：抽取每个角色的新记忆。
4. `relationship_delta`：抽取关系变化，并引用证据。
5. `digest`：生成给用户看的短摘要。

最重要的规则：

**关系变化必须有证据。**

例如不能只输出“许真对灵灵 trust +1”。必须说明是哪句话、哪个行为导致变化。

## 回合制聊天规则

第一版聊天采用简单回合制：

1. 当前角色说一句话。
2. 当前角色指定下一句话对谁说。
3. 被指定的人变成下一轮说话者。
4. 每一轮只调用当前角色的大模型。
5. 一次聊天最多 100 句。

调用大模型时，只放当前角色的信息：

- 当前角色的 `data/robot_sets/{set}/{robot}/role.md`；
- 当前角色的 `data/robot_sets/{set}/{robot}/memory.md`；
- 本次聊天完整记录；
- 输出格式要求。

不要放其他角色的 `role.md`。角色之间的信息不对称是产品乐趣的一部分。

单轮模型输出建议用 JSON：

```json
{
  "speaker": "许真",
  "target": "灵灵",
  "text": "你说这是预兆，我能理解你的不安，但这条新闻现在还缺最基本的事故数据。",
  "mood": "克制但不耐烦"
}
```

如果模型输出非法 JSON，重试一次；第二次失败则停止本次聊天，把 session 标记为 failed。

## 记忆沉淀规则

一次 100 句聊天结束后，每个角色都要沉淀记忆。即使某个角色说话很少，也要根据完整聊天记录形成自己的主观记忆。

记忆沉淀分两步：

1. 为每个角色生成一个 `episodes/{session_id}.md`，记录这个角色如何理解本次聊天。
2. 用旧 `memory.md` 加新 episode，压缩生成新的 `memory.md`。

沉淀记忆时仍然只站在该角色视角。不能写入它没有听到、没有推断依据的“上帝视角真相”。

长期 `memory.md` 控制在 1000 到 2000 汉字以内。它应该保留稳定人格、近期重要关系变化、尚未解决的矛盾，删除琐碎事实。

详细机制见 `聊天与记忆机制.md`。

## 配置

建议使用：

- `config/robots.yaml`：角色设定；
- `config/models.yaml`：模型路由；
- `.env`：本地密钥，不提交到 Git。

模型路由建议：

- 私下反应：便宜快速模型；
- 公开讨论：较强模型；
- 摘要：较强模型；
- 质量检查：较强模型；
- 故事修复：必要时用最强模型。

不要所有步骤都用最强模型。成本会很快失控。

## 页面和接口

最小路由：

- `GET /`：最近摘要列表和创建新闻表单；
- `POST /news`：创建新闻；
- `POST /sessions/{id}/generate`：生成或重试讨论；
- `GET /sessions/{id}`：摘要和完整聊天；
- `POST /sessions/{id}/interventions`：用户插话；
- `GET /robots`：机器人卡片和关系矩阵；
- `GET /robots/{id}`：单个机器人的记忆和关系历史。

首页优先展示：

- 新闻标题；
- 今日冲突；
- 三句话摘要；
- 角色立场；
- 最精彩三句引用；
- 关系变化；
- 隐藏内心戏；
- 一个插话入口。

## 测试计划

至少要有这些测试：

- 数据库迁移能创建全部表和索引；
- 关系更新事务能原子提交；
- 生成失败不会修改关系；
- 模型返回错误 JSON 时会失败并显示可重试；
- 摘要页面包含新闻、引用和关系变化；
- 配置缺少角色或密钥时给出清晰错误；
- 同一时间重复点击生成不会产生两份冲突状态。

## 失败模式

| 路径 | 可能失败 | 处理方式 |
|---|---|---|
| 配置加载 | 缺少模型密钥 | 启动时报清楚 |
| 新闻创建 | 标题为空或正文太长 | 表单错误 |
| 模型调用 | 超时 | session 标记 failed，允许重试 |
| JSON 解析 | 模型输出格式错 | session 标记 failed，保留错误 |
| 关系更新 | 写到一半失败 | 事务回滚 |
| 摘要生成 | 内容很无聊 | 质量分低，提示需要重生成 |
| 并发生成 | 两个请求同时写 | 互斥锁或 busy_timeout |

最大坑：

**如果摘要失败但关系已经更新，就是严重 bug。**

所有写入应该在一个清晰事务边界里完成，或者先写临时结果，最后统一提交。

## 性能判断

第一版性能瓶颈是大模型，不是 SQLite。

注意三点：

- 不要把所有历史记忆都塞进 prompt；
- 每次只加载每个角色最近 N 条记忆和高重要性记忆；
- 摘要物化存储，不要每次打开页面重新算。

## 不在第一版范围内

- 硬件机器人控制：先证明内容循环；
- 自动新闻源：先手动输入；
- 用户账号：本地原型不需要；
- 多租户：没有真实测试用户前不做；
- 向量数据库：先用结构化记忆；
- 漫画和动画：后续内容形态；
- 手机 App：响应式网页足够；
- 语音输入输出：先把文字循环跑通。

## 实现顺序

1. 初始化 Go 项目和基础 HTTP 服务。
2. 添加 SQLite 连接、WAL 设置、迁移系统。
3. 添加角色和模型配置加载。
4. 种子写入 5 个机器人和初始关系。
5. 做手动新闻创建页面。
6. 做假 LLM provider，用于稳定测试。
7. 实现 salon 生成流程。
8. 接入真实 LLM provider。
9. 做摘要页和完整聊天页。
10. 做用户插话。
11. 做质量检查和重试。

## 推荐下一步

先不要写 Go。

先做两件事：

1. 用 `机器人角色卡.md` 里的 5 个角色，拿一条新闻跑 12 轮纯文本模拟。
2. 按角色区分度、关系变化、新闻理解、情感钩子、回访欲望打分。

如果总分低于 18/25，先改角色，不要写代码。
