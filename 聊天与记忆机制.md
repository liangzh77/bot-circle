# 聊天与记忆机制

## 核心目标

让 5 个机器人像一个小社会一样持续聊天。

每个角色只知道自己的角色设定、自己的人生经历、自己的记忆，以及本次聊天到目前为止的完整聊天记录。它不知道其他人的完整人物设定。这样能避免大模型把所有人都写成“上帝视角编剧”，而是让每个角色从自己的局限里说话。

## 聊天方式

采用最简单的回合制：

1. 当前说话者发言。
2. 当前说话者必须指定下一句话是对谁说的。
3. 被指定的人变成下一轮说话者。
4. 每轮只调用当前说话者自己的大模型。
5. 一共说 100 句。
6. 达到 100 句后，本次聊天结束，进入记忆沉淀。

不做“所有人同时思考”。  
不做“主持人判断谁该说”。  
不做复杂群聊调度。

第一版只需要这个链条：

```text
当前角色
  |
  | 读取自己的角色文件
  | 读取自己的记忆文件
  | 读取本次聊天完整记录
  v
调用大模型生成一句话
  |
  | 输出：说话内容 + 指定下一个说话对象
  v
写入聊天记录
  |
  v
下一个角色继续
```

## 每次调用大模型时给什么上下文

只给当前角色这些信息：

1. 自己的角色文件：`data/robot_sets/{set}/{robot}/role.md`
2. 自己的记忆文件：`data/robot_sets/{set}/{robot}/memory.md`
3. 本次聊天完整记录：从第 1 句到当前句
4. 当前任务规则：
   - 你只能扮演自己；
   - 你不能知道其他人的内心和完整设定；
   - 你必须回应当前对话；
   - 你必须指定下一个说话对象；
   - 不要总结，不要旁白，只说一句自然的话。

不放其他人的角色文件。

这点很重要。角色之间的误解、偏见和信息不对称，正是有趣的来源。

## 单句输出格式

模型输出建议用 JSON，方便程序解析。

```json
{
  "speaker": "许真",
  "target": "灵灵",
  "text": "你说这是预兆，我能理解你的不安，但这条新闻现在还缺最基本的事故数据。",
  "mood": "克制但不耐烦"
}
```

字段说明：

- `speaker`：当前说话者，必须等于本轮角色。
- `target`：下一轮说话对象，必须是其他角色之一。
- `text`：本轮发言，一句话，建议不超过 80 个汉字。
- `mood`：可选，用来帮助后续记忆总结。

如果模型输出非法 JSON，重试一次。第二次还失败，本轮标记失败，停止聊天。

## 谁先说话

第一版可以简单处理：

- 如果有新闻输入，让系统指定一个最可能被新闻刺痛的角色先说。
- 如果只是日常闲聊，随机选一个角色。

后面可以做更复杂的“触发点”机制，但第一版没必要。

## 100 句是否太长

100 句适合作为一次完整后台生成，但不适合直接展示给用户。

用户看到的应该是：

- 今日三句话摘要；
- 最精彩三句引用；
- 关系变化；
- 每个角色的私人记忆变化；
- 一个隐藏内心戏；
- 完整聊天记录入口。

100 句是原材料，不是产品界面。

## 记忆系统选择

推荐使用 **轻量分层记忆**。

它参考了几种主流做法：

- MemGPT / Letta 的思路：把记忆分成上下文里的核心记忆和外部长期档案记忆；
- Generative Agents 的思路：用近因、重要性、相关性决定哪些记忆更值得带入上下文；
- 常见轻量 Agent 实践：每轮保留完整聊天记录，会后把聊天压缩成每个角色自己的长期记忆。

但我们第一版不接向量数据库，不做复杂检索。

原因：

- 当前只有 5 个角色；
- 每次聊天 100 句以内；
- 文件系统足够透明；
- 用户可能想手动改角色和记忆；
- 复杂检索会让第一版难以调试。

## 每套角色的文件夹结构

```text
data/robot_sets/
  news_salon/
    qishu/
      role.md
      memory.md
      episodes/
        0001.md
    xuzhen/
      role.md
      memory.md
      episodes/
        0001.md
  deyun/
    guodegang/
      role.md
      memory.md
    yuqian/
      role.md
      memory.md
```

### role.md

稳定角色设定。

包括年龄、籍贯、成长经历、原生家庭、世界观、恐惧、说话风格、社交关系。

一般不由程序自动改，除非用户手动调整。

### memory.md

当前长期记忆摘要。

它是每次聊天后被更新的“压缩记忆”，供下一次聊天直接放进 prompt。

### episodes/*.md

每次聊天结束后，每个角色保存一份自己的事件记忆。

它不是完整聊天记录，而是这个角色如何理解这次聊天。

## 记忆分三层

### 1. 本次工作记忆

就是本次聊天的完整 100 句记录。

它只在本次聊天中完整放进 prompt。聊天结束后不再每次完整塞入上下文。

### 2. 情节记忆

每次聊天结束后，为每个角色生成一份 episode。

每个角色都要生成，即使这次没说几句话，也要总结它听到的内容和自己的感受。

格式建议：

```markdown
# 记忆片段 0001

## 发生了什么

今天大家围绕某条新闻争论了……

## 我注意到

- 旗叔把这件事理解成尊严问题。
- 灵灵其实是在担心人们太相信系统。

## 我对其他人的感觉

- 对旗叔：更不耐烦，但承认他不是完全没有道理。
- 对灵灵：仍然觉得她证据不足，但她确实看到了情绪层面的风险。

## 我自己的变化

我发现自己又一次用“缺少数据”回避了对恐惧的承认。

## 重要性

4/5
```

### 3. 长期摘要记忆

`memory.md` 是长期摘要。

每次聊天结束后，用旧的 `memory.md` 加上新的 episode，压缩成新的 `memory.md`。

它应该短，最好控制在 1000 到 2000 汉字以内。

内容包括：

- 我是谁；
- 我最近在意什么；
- 我对每个人的稳定印象；
- 最近几次聊天留下的情绪；
- 我尚未解决的矛盾；
- 我不愿承认但越来越明显的变化。

## 记忆沉淀流程

一次 100 句聊天结束后：

```text
完整聊天记录
  |
  v
对每个角色分别调用模型
  |
  | 输入：该角色 role.md + memory.md + 完整聊天记录
  | 输出：该角色自己的 episode 记忆
  v
写入 data/robot_sets/{set}/{robot}/episodes/{session_id}.md
  |
  v
再次对每个角色调用模型
  |
  | 输入：旧 memory.md + 新 episode
  | 输出：新的压缩 memory.md
  v
覆盖 data/robot_sets/{set}/{robot}/memory.md
```

注意：沉淀记忆时仍然只站在该角色视角。

比如许真可以记录“我觉得灵灵在逃避证据”，但不能记录“灵灵其实因为童年孤独才这么说”，除非灵灵在聊天里说出来了。

## 记忆更新规则

长期记忆不能无限增长。

每次更新 `memory.md` 时遵守：

1. 保留稳定人格，不要改掉核心设定。
2. 保留最近重要关系变化。
3. 删除已经不重要的琐碎事实。
4. 如果新记忆和旧记忆冲突，写成“我曾经以为……但最近开始怀疑……”。
5. 不要把别人的私密设定写进自己的记忆，除非聊天里公开暴露。

## Token 控制

每轮聊天调用只放：

- 当前角色 `role.md`；
- 当前角色 `memory.md`；
- 本次聊天记录。

不放：

- 其他角色 `role.md`；
- 所有历史 episode；
- 数据库里所有旧聊天；
- 完整世界设定长文。

如果 100 句聊天记录太长，后续可以改成：

- 最近 30 句完整记录；
- 前面 70 句滚动摘要。

第一版先完整放 100 句，简单直接。

## 关系变化怎么处理

记忆文件负责主观记忆。

关系数值可以后续单独做结构化文件或 SQLite 表，比如：

```json
{
  "qishu": {
    "xuzhen": {
      "trust": -1,
      "respect": 1,
      "irritation": 2,
      "reason": "许真总是质疑旗叔的大词，但旗叔也承认他冷静。"
    }
  }
}
```

第一版可以先不做数值系统，只在 `memory.md` 里写自然语言关系变化。等聊天跑起来后，再决定是否需要数值化。

## 推荐实现顺序

1. 定义聊天 session 文件格式。
2. 实现读取 5 个角色文件夹。
3. 实现回合制聊天：当前角色输出 `target + text`。
4. 保存完整 100 句聊天记录。
5. 实现每个角色的 episode 记忆沉淀。
6. 实现 `memory.md` 压缩更新。
7. 再考虑摘要、关系数值和网页展示。

## 最小可验证版本

最小版本不用 SQLite 也能先跑：

- 输入一条新闻；
- 生成 `sessions/0001/chat.md`；
- 每个角色生成 `episodes/0001.md`；
- 每个角色更新自己的 `memory.md`。

如果这个文件版能产生有趣的连续变化，再把 session 和关系事件落进 SQLite。

## 当前命令行程序

已经实现 `cmd/chatrun`。

完整运行一次 100 句聊天，并在结束后为每个角色沉淀记忆：

```powershell
go run ./cmd/chatrun -news "山西煤矿事故已致90人死亡"
```

指定起始角色：

```powershell
go run ./cmd/chatrun -news "山西煤矿事故已致90人死亡" -start 旗叔
```

指定另一套角色，例如郭德纲和于谦：

```powershell
go run ./cmd/chatrun -set deyun -news "山西煤矿事故已致90人死亡"
```

只跑 2 句，用来检查提示词和回合机制，不更新记忆：

```powershell
go run ./cmd/chatrun -news "山西煤矿事故已致90人死亡" -turns 2 -skip-memory
```

程序会生成：

```text
sessions/{时间}/chat.md
sessions/{时间}/turns.json
sessions/{时间}/prompts/turn-001-*.md
sessions/{时间}/prompts/turn-002-*.md
```

如果不加 `-skip-memory`，还会为每个角色生成：

```text
data/robot_sets/{set}/{robot}/episodes/{session_id}.md
```

并覆盖更新：

```text
data/robot_sets/{set}/{robot}/memory.md
```

`app.config.json` 里必须填写：

```json
{
  "siliconFlow": {
    "apiKey": "你的 SiliconFlow API Key",
    "textModel": "deepseek-ai/DeepSeek-V3.2"
  },
  "robotSetsDir": "data/robot_sets",
  "robotSet": "news_salon"
}
```
